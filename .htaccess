# =======================================================
# 🌐 Minimal Config for XAMPP Testing
# =======================================================

# Enable rewrite engine
<IfModule mod_rewrite.c>
    RewriteEngine On
    DirectoryIndex index.html
</IfModule>

# Allow Override for .htaccess (already set in httpd.conf, but good practice)
# <Directory "/path/to/ImSalione Website">
#     AllowOverride All
# </Directory>